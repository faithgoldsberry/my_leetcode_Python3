class Solution:
    def isPalindrome(self, x: int) -> bool:
        x = str(x)
        reverse = x[::-1]
        for i in range(len(x)):
            if x[i] == reverse[i]:
                return True
            else:
                return False
